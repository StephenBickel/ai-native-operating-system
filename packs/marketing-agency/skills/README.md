# Meridian skills

Each child directory contains one executable business procedure. Agents load only the matching `SKILL.md`, verify permissions before writes, and report tool record IDs in the output.
