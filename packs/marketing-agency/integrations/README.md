# Meridian integrations

These six contracts describe exactly what the demo can read and write. Live credentials are supplied by the deployment owner; mock mode uses `../demo/fixtures/` and never reaches a third party.
