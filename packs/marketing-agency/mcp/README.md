# MCP examples

These examples show remote HTTP MCP for hosted deployments and local `stdio` MCP for private workers. Copy one to deployment configuration and replace placeholder environment-variable references, never literal secrets.
