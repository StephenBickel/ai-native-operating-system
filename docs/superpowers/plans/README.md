# Implementation plans

Plans in this directory translate approved specifications into testable, reviewable build tasks. Agents execute them in order, record deviations in the relevant plan, and preserve completed plans as implementation provenance.
