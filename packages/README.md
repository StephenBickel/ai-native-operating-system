# Runtime packages

Packages are small, independently tested parts of the deployable product. They may depend on core contracts but never on a specific industry pack. Each package exposes its public API from `src/index.ts`.
