# Operating-system loader

Loads the universal core and one active pack, validates their contracts, and resolves restrictive governance overlays. It reads repository content and never mutates packs during runtime.
