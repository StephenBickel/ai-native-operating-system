# Database migrations

Migrations are ordered, forward-only SQL files. They must preserve customer data, enable row-level security, and document any deployment action that cannot run transactionally.
