# Database package

Defines the portable PostgreSQL schema and connection boundary for collaborative runtime state. Application code reads and writes through workspace-scoped queries; migrations are reviewed, forward-only records.
