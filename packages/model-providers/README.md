# Model providers

This package presents one streaming interface across deterministic demo inference and customer-selected model APIs. Providers receive already-governed context and never receive raw connector credentials.
