# Storage adapters

Stores user attachments and generated artifacts behind a portable interface. Local storage is restricted to its configured root; production deployments may add an S3-compatible adapter.
