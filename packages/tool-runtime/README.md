# Tool runtime

Registers typed operations and enforces governance before any connector executes. It validates inputs, redacts audit data, returns durable approval requests, and denies forbidden operations independently of model instructions.
