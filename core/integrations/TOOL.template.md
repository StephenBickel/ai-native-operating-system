# Tool name

## Reads

- `resource.operation`: fields returned and business purpose.

## Writes

- `resource.operation`: effect, idempotency strategy, and default autonomy tier.

## Used by skills

- `skill-name`

## Setup

Document transport, endpoint, credential reference, scopes, webhook verification, and connectivity test.

## Permissions

Map each operation to allowed roles, data classifications, autonomy tier, and approval rule.

## Mock mode

Name fixture inputs, deterministic outputs, and differences from a live account.

## Failure policy

Define timeouts, retry eligibility, rate-limit behavior, reconciliation, escalation, and redacted audit fields.
