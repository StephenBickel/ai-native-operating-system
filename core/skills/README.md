# Skill contract

A skill is an executable operating procedure stored in its own directory as `SKILL.md`. Agents select a skill when its trigger matches, read its integration contracts, resolve the required autonomy tier, and follow its ordered steps. A pack owns industry-specific skills; core owns only the format.
