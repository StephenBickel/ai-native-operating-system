# GitHub operations

Issue forms create a human-and-agent task queue, `labels.yml` documents lifecycle labels, and workflows validate contracts or produce reviewable operational artifacts. Workflows never receive live demo-tool secrets.
