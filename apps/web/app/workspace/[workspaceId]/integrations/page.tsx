import { Cable } from "lucide-react";
import { SectionPage } from "../../../../components/section-page";
export default function IntegrationsPage() { return <SectionPage icon={Cable} title="Integrations" description="Customer-owned tools with operation-level permissions." items={["HubSpot — connected", "Gmail — connected", "Slack — connected", "Google Drive — connected", "Stripe — mock mode"]} />; }
