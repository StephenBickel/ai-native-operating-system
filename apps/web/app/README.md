# Application routes

Routes define public, authentication, workspace, and server API surfaces. Workspace pages receive an authenticated workspace identifier and compose focused components; route handlers validate membership again before reading or writing data.
