# Web application

The Next.js application is the shared control plane for chat, tasks, approvals, automations, knowledge, integrations, runs, and administration. Server routes authenticate workspace access before calling runtime packages; client components own only local interaction state.
